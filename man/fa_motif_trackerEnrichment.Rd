% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/motifTracker_analyses.R
\name{fa_motif_trackerEnrichment}
\alias{fa_motif_trackerEnrichment}
\title{Compute enrichment of specific motifs or sequences across populations}
\usage{
fa_motif_trackerEnrichment(trackerDF = NULL)
}
\arguments{
\item{trackerDF}{A data.frame from \code{fa_motif_motifTracker} or \code{fa_motif_sequenceTracker}}
}
\value{
A data.frame with enrichment scores for each query
}
\description{
\code{fa_motif_trackerEnrichment} computes the enrichment of user-defined motifs or sequences from \code{fa_motif_motifTracker} or \code{fa_motif_sequenceTracker}.
}
\examples{
\dontrun{
# count FASTQ files
countData <- list(
  fa_count(dataInput = "PATH/TO/FILE1.fastq"),
  fa_count(dataInput = "PATH/TO/FILE2.fastq")
)

# count motifs in input files
trackerData <- fa_motif_motifTracker(
  faDF_list = countData,
  populationNames = c("Population A", "Population B"),
  queryList = c("AAA", "AACG"),
  queryAliases = c("motif1", "motif2"),
  motifType = "Nucleotide"
)

# compute motif enrichment
enrichData <- fa_motif_trackerEnrichment(trackerDF = trackerData)
}

}
\seealso{
\code{\link[=fa_motif_motifTracker]{fa_motif_motifTracker()}}, \code{\link[=fa_motif_sequenceTracker]{fa_motif_sequenceTracker()}}
}
